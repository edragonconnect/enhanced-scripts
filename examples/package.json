{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "enhanced-scripts build  --use-config config.yaml",
    "start": "enhanced-scripts start --dist _build --src  apps",
    "clean": "rm -rf node_modules/enhanced-scripts && yarn upgrade enhanced-scripts &&  rm -rf _build && yarn build"
  },
  "ignored_paths": [
    "project_42"
  ],
  "dependencies": {
    "enhanced-scripts": "file:.."
  },
  "homepage": "/_build"
}
